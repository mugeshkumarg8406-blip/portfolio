export default function About() {
  return (
    <section id="about">
      <div className="eyebrow">About</div>
      <div className="card">
        <p>
          Building scalable server-side systems and robust APIs. Focused on Python, Java,
          and cloud infrastructure — the parts of a product nobody sees, working exactly
          as they should.
        </p>
      </div>
    </section>
  )
}
